package ir.metrix.notification;

public class MetrixNotification {

    /**
     * Check if Notification registration has successfully been performed
     *
     * @return True if the user has been registered with Notification and false if registration
     * has not been performed yet
     */
    public static boolean isRegistered() {
        return false;
    }

    /**
     * Check if Notification initialization has successfully completed
     *
     * @return True if Notification is initialization is complete, false otherwise
     */
    public static boolean isInitialized() {
        return false;
    }
}
